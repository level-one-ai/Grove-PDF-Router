{
  "version": 2,
  "functions": {
    "api/*.js": {
      "maxDuration": 60
    }
  },
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 9 * * *"
    }
  ],
  "routes": [
    { "src": "/dashboard", "dest": "/api/dashboard" },
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
